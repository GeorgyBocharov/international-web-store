spring:
  application:
    name: compositor-api

server:
  port: 8082

management:
  endpoints:
    web:
      exposure:
        include: "*"

eureka:
  client:
    serviceUrl:
      defaultZone: "http://host.docker.internal:8761/eureka"